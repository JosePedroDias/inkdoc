<div class="box box-class">

    <h2><a name="{{{hash}}}">Class: {{name}}</a></h2>

    <p>Defined in file: {{file}}</p>

    {{#if version}}
        <p><label>version:</label>{{version}}</p>
    {{/if}}

    {{#if text}}
        <p class="text">{{{text}}}</p>
    {{/if}}

    {{> examples}}

    <h3>Defined methods:</h3>
    <ul>
    {{#each methods}}
        <li><a href="#{{{hash}}}">{{parent.name}}.{{name}}()</a></li>
    {{/each}}
    </ul>

    {{#each methods}}
        {{> method}}
    {{/each}}

</div>
